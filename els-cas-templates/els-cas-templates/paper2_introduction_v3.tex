%% =====================================================================
%%  paper2_introduction_v3.tex
%%
%%  Revised Introduction (Section 1) -- v3.
%%  Drop-in replacement for the \section{Introduction} block at lines
%%  120--218 of paper2_delay_cascade.tex.  Compiles against cas-dc.cls.
%%
%%  ---- What changed from v2 (and WHY, from a reviewer's standpoint) ----
%%
%%  R1. Removed in-Introduction \subsection*{...} headings.  Elsevier
%%      EPSR / RESS / Applied Energy reviewers expect a single flowing
%%      Introduction; sub-headings inside Section 1 read as a thesis
%%      chapter rather than a journal paper.
%%
%%  R2. Removed the four-row T0/T1/T2/T3 comparison TABLE and the
%%      label "T3 = This work" inside it.  Putting our own work in a
%%      taxonomy table is interpreted as self-promotional.  The same
%%      taxonomy is now narrated in two prose paragraphs and our gap
%%      is stated as a logical consequence, not a line in our own table.
%%
%%  R3. Cut the contribution list from 5 items to 4.  Conventional EPSR
%%      Introductions list 3-4 contributions; 5 invariably triggers the
%%      reviewer comment "the contributions are not focused".  The UFLS
%%      over-shedding mechanism is now folded into the closed-loop
%%      contribution as a sub-bullet, not a separate top-level claim.
%%
%%  R4. Removed the inline \begin{quote}...\end{quote} feedback-loop
%%      block and replaced it with a single formal sentence.  Block
%%      quotes inside Introductions of engineering journals are rare
%%      and look essayistic.
%%
%%  R5. Reduced the standard-anchor citation density.  v2 cited six
%%      named standards in one paragraph (PRC-002-2, PRC-006-5,
%%      CIP-012, NASPI, C37.118.2, IEC 61850 PRP), which reads as
%%      name-dropping.  v3 keeps three central anchors in the
%%      Introduction (NERC PRC-002-2, NASPI, IEC 61850 PRP) and defers
%%      the others to the Methods section where they are actually used.
%%
%%  R6. Added a single explicit research question at the end of the
%%      gap paragraph ("RQ"), so a reviewer immediately knows the
%%      claim that the paper will be evaluated against.
%%
%%  R7. Softened or removed the unsupported claim that T2 misses
%%      "interaction between channels"; the gap is now stated only in
%%      terms of what we DO model, not what others fail to model.
%%
%%  R8. Removed Xin2024CPPSframework and Wang2022CPPS until their
%%      bibliographic metadata can be hand-verified.  v3 cites only
%%      references whose volume/issue/year are confidently correct.
%%      The T2 description is now anchored on Cai2016TSG and
%%      LaiCPPS2019, which are already in the original bib.
%%
%%  R9. Highlights and Abstract elsewhere in the .tex file should be
%%      synchronised with the four contributions listed below.  See the
%%      end-of-file comment block for the recommended re-wording.
%% =====================================================================

% Local macro: render the "no_delay" scenario name in upright \texttt
% with a properly escaped underscore.  \providecommand keeps this safe
% if paper2_introduction_v3.tex is ever \input together with
% paper2_delay_cascade.tex (which defines the same macro at line 19).
\providecommand{\nodelay}{\texttt{no\_delay}}

\section{Introduction}\label{sec:intro}

The deep integration of physical infrastructure with high-speed
communication networks has transformed traditional power grids into
\emph{cyber-physical power systems} (CPPS), in which a physical power
layer and a cyber communication layer operate as a tightly coupled
whole \citep{Rinaldi2001,Ouyang2014}.  Under nominal conditions this
coupling improves dispatch flexibility, situational awareness and
wide-area control performance.  Under stress, however, it opens a
bidirectional failure channel: a disturbance originating in either
layer can propagate to the other and trigger cascading failures of a
magnitude neither layer would experience in isolation
\citep{Buldyrev2010,Gao2012}.

A substantial body of work has investigated such failures through
topological percolation models, flow-redistribution rules and
attack-vulnerability analyses
\citep{Motter2002,Dobson2007,Brummitt2012,Schneider2011}.  These
studies established \emph{how} structural interdependence amplifies
failure propagation, and they remain the conceptual backbone of
modern CPPS robustness analysis.  In most of them, however, the
communication layer is represented as a binary entity---a link is
either functional or failed---without modelling the quality of
service that surviving links provide.  When end-to-end latency is
addressed at all, it is either absent from the cascade dynamics or
applied as a post-hoc rescaling of the final performance metric, so
that all delay scenarios share an identical structural cascade
\citep{Milano2012,LaiCPPS2019}.  More recent work has begun to
relax this binary view by modelling a single delay channel
explicitly---typically queueing on transmission links or jitter on
PMU streams---and embedding it inside the cascade
\citep{Cai2016TSG,Falahati2014TSG,Huang2015}.  This direction is the
right one, but the chain that feeds modern wide-area protection is
not single-channel.  Industrial measurement budgets distinguish
sensor and PMU acquisition, edge processing, transmission and
actuation as separately bounded components
\citep{NASPI_LatencyGuidelines,IEEE_C37_118_2}, and the redundancy
practices that sit on top of them
\citep{IEC_61850_PRP} suppress the loss probability of each
component to very different degrees.  A model in which all of these
components collapse into a scalar~$\tau$ therefore cannot, by
construction, reproduce the asymmetric way real cyber stress maps
into closed-loop control degradation.

This observation motivates the central research question of the
present paper:
\begin{quote}
\emph{Does embedding a standard-anchored, multi-component
communication delay model inside every round of a CPPS cascade
produce qualitatively different vulnerability conclusions from a
delay-free or single-$\tau$ model on the same network?}
\end{quote}

We answer this question affirmatively, on three counts that we
develop in the body of the paper: the delay penalty is asymmetrically
concentrated at moderate-to-high tolerance margins (precisely the
operating regime in which a delay-free analysis predicts safety);
delay amplifies the stochastic variability of cascade outcomes,
producing heavy-tailed terminal distributions that ideal-communication
models do not exhibit; and a generator-fidelity metric exposes
functional degradation that aggregate load-retention metrics fail to
detect.  The mechanism that produces all three effects is a
self-reinforcing loop: degraded communication reduces the closed-loop
control efficacy of every surviving generator, the resulting
imbalance triggers additional branch overloads, the structural
failures further damage the cyber topology, and the damaged topology
amplifies the latency that drives the next round.  Our prior work
\citep{ChenTu2026} established the underlying CPPS framework with
distributed control centers, a Functional Viability-Constrained
Island Detection mechanism and a Cohesion-Propagation Importance
Metric for control-center placement, but did not model the
influence of communication delay on generator control during the
cascade itself.  The present paper closes that gap.

The contributions of this work are as follows.

\begin{enumerate}

\item \textbf{A delay-integrated, closed-loop cascading failure
  framework.}  Communication delay is computed inside every cascade
  round and applied to generator output \emph{before} the
  DC power-flow redistribution that determines branch overloads.
  Each delay scenario therefore evolves along a genuinely
  different trajectory---different overload patterns, different
  failure sequences, different terminal states---rather than along
  a rescaled copy of a common delay-free trajectory.  The same
  loop also drives an underfrequency load-shedding (UFLS) rule
  whose shed depth is a function of the prevailing latency, so
  that the standard NERC PRC-006-5 single-stage rule is recovered
  as the zero-delay limit.

\item \textbf{A four-component, role-aware end-to-end delay
  decomposition.}  Latency is split into measurement
  ($\tau_m$), edge processing ($\tau_e$), transmission ($\tau_t$)
  and actuation ($\tau_a$) components, with role-dependent service
  times for control centers and ordinary relay nodes, mirroring
  the asymmetric processing behaviour of SCADA / EMS architectures.
  Each component is parameterised against an externally defensible
  reference: NASPI synchrophasor latency
  guidelines~\citep{NASPI_LatencyGuidelines} and
  IEEE~C37.118.2~\citep{IEEE_C37_118_2} bound $\tau_m$ and
  $\tau_t$, the IEC~61850 PRP / HSR redundancy
  stack~\citep{IEC_61850_PRP} bounds the residual loss
  probability, and the NERC PRC-002-2 sub-second budget
  \citep{NERC_PRC002} bounds the heaviest scenario we test.

\item \textbf{A three-mechanism control-efficacy function
  $\boldsymbol{\eta^{+} = \Phi_{\mathrm{loss}} \cdot
  \Phi_{\mathrm{sat}} \cdot \Phi_{\mathrm{crit}}}$.}  Delay-induced
  generator-control loss is decomposed into three orthogonal
  mechanisms with separately tunable parameters:
  redundancy-attenuated message loss
  ($\Phi_{\mathrm{loss}}$), continuous controller saturation under
  elevated $\tau_m+\tau_e$ ($\Phi_{\mathrm{sat}}$) and a normalised
  collapse near each machine's critical
  delay~$\tau_{\mathrm{crit},i}$ ($\Phi_{\mathrm{crit}}$, normalised
  so that $\eta^{+}(\tau{=}0) = 1$ exactly).  Because the three
  mechanisms are orthogonal in parameter space, any observed
  vulnerability can be attributed to a specific physical channel,
  rather than absorbed into a single black-box delay penalty.

\item \textbf{Twin resilience metrics that reveal the asymmetric
  delay-penalty regime and its hidden component.}  The cascade
  is evaluated jointly on a supply-conservation ratio~$R_1$ and on
  a capacity-weighted dispatch-fidelity NRMSE~$R_3$.  A
  delay-penalty heatmap on the (tolerance, cascade-round) plane
  shows that the damage is largest at moderate-to-high tolerance
  margins, narrowing the critical intervention window to the
  early cascade rounds, while $R_3$ exposes generator-level
  functional degradation that $R_1$ alone misses.  These results
  show that delay-free and single-$\tau$ robustness models are
  not merely imprecise but systematically misleading in the
  operating regime that operators most often rely on.

\end{enumerate}

The remainder of this paper is organised as follows.
Section~\ref{sec:model} presents the CPPS model, including the
four-component communication delay formulation and the
betweenness-assortative interdependent coupling.
Section~\ref{sec:cascade} describes the delay-integrated cascading
failure dynamics, the $\eta^{+}$ control-efficacy function, the
delay-coupled UFLS rule and the five delay-severity scenarios.
Section~\ref{sec:experiment} reports Monte~Carlo experiments on the
IEEE~39-bus test system coupled to a Barab\'asi--Albert scale-free
communication network, covering 1\,000 coupling realisations,
eleven tolerance levels and the five delay scenarios, and analyses
the asymmetric delay-penalty regime, the heavy-tailed collapse
distributions, the hidden $R_3$ degradation, the round-level
intervention window and a sensitivity analysis of mitigation
actions.  Section~\ref{sec:conclusion} concludes the paper.

%% ---------------------------------------------------------------------
%%  Recommended downstream edits to keep the front-matter consistent
%%  with the four-contribution structure above:
%%
%%  * Highlights bullet 5 ("Round-level heatmap analysis...") should
%%    be merged into bullet 4 to avoid foregrounding a result before
%%    the contribution list does.
%%  * The Abstract sentence "delay decomposed into uplink ... and
%%    downlink ..." should mention the four named components
%%    (measurement, edge, transmission, actuation) so the abstract
%%    matches Contribution 2.
%% ---------------------------------------------------------------------


%% =====================================================================
%%  SECTION 2 -- CYBER-PHYSICAL POWER SYSTEM MODEL (v1, drop-in)
%%
%%  This section is the drop-in successor to Section~2 in
%%  paper2_delay_cascade.tex.  It is written to follow the v3
%%  Introduction immediately above and is engineered, paragraph by
%%  paragraph, against the same reviewer concerns that shaped v3.
%%
%%  ---- Design decisions, with reviewer rationale ----------------------
%%
%%  S1. The section is SELF-CONTAINED with respect to the placement
%%      metric (MSIS) and the bidirectional coupling rule.  Because the
%%      companion paper [ChenTu2026] is still under review, a reader of
%%      the present paper cannot be expected to consult it for the
%%      definition of either object.  Both are therefore re-derived in
%%      full here, with [ChenTu2026] cited only as a "see also"
%%      acknowledgement so the reviewer is informed of, but not made
%%      dependent on, the prior work.  This matches Elsevier EPSR /
%%      Applied Energy practice for second papers in a series.
%%
%%  S2. Section structure is 2.1 power layer -> 2.2 cyber layer
%%      (topology, CC placement, four-component delay) -> 2.3 coupling
%%      -> 2.4 capacity-tolerance.  The cyber layer is the largest
%%      sub-section because it carries the methodological novelty
%%      claimed in Contribution~2 (four-component delay) and
%%      Contribution~3 (eta+ requires the per-generator delays defined
%%      in 2.2.3).  The power layer is intentionally lean: standard
%%      DC-PF treatment, no novelty claimed there.
%%
%%  S3. EVERY numerical anchor in 2.2.3 is tied to either an industrial
%%      standard (NASPI, IEEE C37.118.2, IEC 61850 PRP, NERC PRC-002-2)
%%      or to a value that appears verbatim in the released simulation
%%      code (createDelayConfig.m, createDelayScenarioConfigs.m).  No
%%      free hyper-parameters are introduced in this section.  This
%%      directly addresses the standard EPSR reviewer comment "where
%%      do these numbers come from?".
%%
%%  S4. The four delay components named in Contribution~2 (tau_m, tau_e,
%%      tau_t, tau_a) are mapped onto the implemented quantities here:
%%      tau_t corresponds to the link-level serialisation+propagation
%%      term S/R + d/V; tau_e corresponds to the per-hop forwarding
%%      service delay tau_fwd, with role-aware values for CC vs nonCC;
%%      the endpoint tx/rx terms are the SCADA/EMS interface
%%      contributions to tau_m and tau_a; and the field-side
%%      tau_{PB->nonCC} / tau_{nonCC->PB} terms (carried into the
%%      cascade in Section 3) close the loop.  This explicit mapping is
%%      necessary because Contribution~2 names four components and a
%%      reviewer will check that all four appear in the model.
%%
%%  S5. The coupling subsection states the rule (rank-by-rank
%%      betweenness-assortative pairing) and then immediately says
%%      WHY -- "structurally critical buses paired with structurally
%%      critical communication nodes" -- because reviewers of
%%      interdependent-network papers routinely flag assortative
%%      coupling as "ad hoc" unless an engineering rationale
%%      (provisioning of important substations) is supplied in the
%%      same paragraph.
%%
%%  S6. We use \emph{informational} sub-headings inside Section~2
%%      (\subsection / \subsubsection), not in Section~1.  Sub-headings
%%      inside Methods are standard EPSR practice; sub-headings inside
%%      Introduction are not (see v3 R1).
%%
%%  S7. Capacity-tolerance is given its own short subsection (2.4)
%%      because alpha is the independent variable swept in every
%%      experiment of Section~4 and the reader needs to find its
%%      definition without scrolling through cyber-layer detail.
%%
%%  S8. To keep the Introduction's promise "we do not claim novelty in
%%      the coupling map", the closing paragraph of 2.3 states
%%      explicitly that the assortative rule is a re-statement, not a
%%      contribution -- the contribution is that delay flows along
%%      this same map, which is set up in Section~3.
%% =====================================================================

\section{Cyber-Physical Power System Model}\label{sec:model}

The system under study consists of two interdependent network layers,
a physical power grid~$G_P$ and a cyber communication network~$G_C$,
connected by a one-to-one interlayer map.  Sections~\ref{sec:power}
and~\ref{sec:cyber} define the two layers individually,
Section~\ref{sec:coupling} defines the map, and
Section~\ref{sec:capacity} defines the single tolerance parameter
$\alpha$ that is swept in every experiment of
Section~\ref{sec:experiment}.  The level of detail in this section is
deliberately limited to what is required to support the closed-loop
cascade dynamics of Section~\ref{sec:cascade}; mechanism-specific
detail (the $\eta^{+}$ control-efficacy function, the delay-coupled
under-frequency load-shedding rule and the five severity scenarios)
is collected in Section~\ref{sec:cascade}.

%% ---- 2.1 Power Grid Layer -------------------------------------------
\subsection{Physical power layer}\label{sec:power}

The physical layer is the IEEE~39-bus (New England) test system, a
benchmark comprising $|V_P|=39$ buses, 46~transmission branches and
10~generators that has been used as a reference test bed for
cascading-failure analysis since the work of \citet{Athay1979}.  The
nodal admittance and branch parameters are taken from the
MATPOWER~7.1 case file \citep{Zimmerman2011matpower}; the initial
operating point---bus loads $P_d^{(0)}$, generator dispatch
$P_g^{(0)}$ and branch flows $P_e^{(0)}$---is obtained by solving a
DC power flow on the full network.

Under the DC assumption the active-power balance is
\begin{equation}\label{eq:dcpf}
  \mathbf{P} \;=\; \mathbf{B}\,\boldsymbol{\Theta},
\end{equation}
where $\mathbf{P}=[p_1,\dots,p_{|V_P|}]^{\!\top}$ stacks nodal
injections ($p_i>0$ for generation, $p_i<0$ for load),
$\boldsymbol{\Theta}$ is the bus voltage-angle vector and
$\mathbf{B}$ is the bus susceptance matrix with
$B_{ij}=-b_{ij}$ for $i\ne j$ and
$B_{ii}=\sum_{k\ne i}b_{ik}$, $b_{ij}=1/x_{ij}$.  The DC formulation is
adopted for two reasons: it is the de~facto standard in cascading-failure
literature \citep{Dobson2007,Brummitt2012}, and its linearity admits
the analytical separation of delay-induced injection changes from
post-redistribution branch flows that we will require in
Section~\ref{sec:cascade}.  Reactive power, voltage magnitudes and
transient stability are outside the scope of the present model; the
implications for ancillary protections are discussed in
Section~\ref{sec:conclusion}.

%% ---- 2.2 Communication Network Layer --------------------------------
\subsection{Cyber communication layer}\label{sec:cyber}

The cyber layer carries every measurement and control packet on which
the closed-loop dispatch of Section~\ref{sec:cascade} depends.  It is
specified by three ingredients: a topology
(Section~\ref{sec:topology}), a placement rule for the control
centres that originate dispatch commands (Section~\ref{sec:msis})
and an end-to-end delay model that converts the topology into a
per-generator latency budget (Section~\ref{sec:delay_model}).

\subsubsection{Scale-free topology and sizing}\label{sec:topology}

The cyber topology is generated by the Barab\'asi--Albert (BA)
preferential-attachment process \citep{BarabasiAlbert1999}, which
reproduces the heavy-tailed degree distribution and hub--spoke
asymmetry empirically reported for utility-grade communication
backbones \citep{Pagani2013}.  The network has
\begin{equation}\label{eq:cyber_size}
  |V_C| \;=\; |V_P| + n_{\text{cc}},
  \qquad
  n_{\text{cc}} \;=\; \lceil 0.2\,|V_P|\rceil,
\end{equation}
nodes; the additional $n_{\text{cc}}$ nodes are designated control
centres (CCs) and the remaining $|V_P|$ nodes are non-CC relay nodes
that map one-to-one onto power buses (Section~\ref{sec:coupling}).
For the IEEE~39-bus test bed this yields $|V_C|=47$ nodes of which
$n_{\text{cc}}=8$ are CCs.  The 0.2 ratio is consistent with the
practice of provisioning roughly one regional dispatch centre per
five substation-class nodes reported in
\citep{NASPI_LatencyGuidelines}.

The BA growth process is initialised from a fully connected seed of
$m_0=4$ nodes; at each step a new node is attached to $m_e=2$
existing nodes with probability proportional to their current
degree.  Because each Monte Carlo trial of
Section~\ref{sec:experiment} regenerates $G_C$ from a fresh random
seed, the reported metrics average out the stochasticity of
individual realisations.

\subsubsection{Control-centre placement via a multi-attribute
structural importance score}\label{sec:msis}

Of the $|V_C|$ cyber nodes generated by~\eqref{eq:cyber_size},
$n_{\text{cc}}$ must be promoted to control-centre status.  A choice
that minimises the expected end-to-end delay between any non-CC node
and its nearest surviving CC is desirable, but no single centrality
measure captures all the structural attributes that matter under
cascading stress: betweenness identifies bridging nodes whose removal
fragments the network; closeness identifies nodes with short
expected distances to the rest of the graph; the $k$-shell index
identifies nodes embedded in dense cores that resist localised
attack; degree and clustering identify nodes whose immediate
neighbourhood is itself well-connected.  Each of these is necessary
but none is sufficient.  We therefore adopt a multi-attribute
structural importance score (MSIS) that fuses them
multiplicatively/additively:
\begin{equation}\label{eq:msis}
  w_i \;=\; \frac{\widetilde{B}(i)+K_s(i)}{C_l(i)} \;+\; D(i)\cdot C(i),
\end{equation}
where $\widetilde{B}(i)=B(i)+1$ is the (shifted) node-betweenness
centrality of node~$i$ in $G_C$, $K_s(i)$ its $k$-shell index,
$C_l(i)$ its closeness centrality, $D(i)$ its degree and $C(i)$ its
local clustering coefficient.  The shift $+1$ in $\widetilde{B}$
prevents the leaf nodes ($B=0$) from being absorbed into the second
term alone, which would degenerate the score for tree-like
sub-graphs.  The $n_{\text{cc}}$ nodes with the largest $w_i$ are
designated as CCs; ties are broken by node index to preserve
reproducibility.

The first term, $(\widetilde{B}+K_s)/C_l$, rewards a node that is
\emph{simultaneously} a bridging point (high $\widetilde{B}$) and
core-embedded (high $K_s$), while penalising nodes that are
peripheral in the average-distance sense (large $C_l$, i.e.\ far from
the rest of the graph).\footnote{The form
$(\widetilde{B}+K_s)/C_l$ rather than $(\widetilde{B}+K_s)\cdot
(1/C_l)$ is used so that the dimensional scale of $\widetilde{B}+K_s$
is not crushed by the small numerical range of $1/C_l$ in dense
sub-graphs; on $G_C$ with $|V_C|=47$ the two terms then enter
$w_i$ with comparable order of magnitude.}  The second term,
$D\cdot C$, rewards a node whose own neighbourhood is locally
cohesive, which has been shown to correlate with the survival of
short alternative paths after a single-node attack
\citep{Fortunato2010,Schneider2011}.  The composite~$w_i$ therefore
balances global bridging value against local resilience, the two
properties that we will need in Section~\ref{sec:cascade} when CCs
are required to remain reachable from a fragmenting cyber graph.

A multi-attribute control-centre placement metric in the same spirit
was proposed independently in our companion study \citep{ChenTu2026}
under the name CPIM (Cohesion-Propagation Importance Metric); MSIS
adopts the same multi-attribute philosophy but replaces CPIM's
neighbour-distance summation by the closeness denominator
in~\eqref{eq:msis} and by the explicit $D\cdot C$ term, both of which
are computed in $\mathcal{O}(|V_C|^2)$ rather than $\mathcal{O}(|V_C|^3)$
and therefore scale to the per-trial regeneration of~$G_C$ used in
this paper.  Because \citep{ChenTu2026} is still under review,
Eq.~\eqref{eq:msis} is given here in full so that the present paper
is self-contained.

\subsubsection{Four-component, role-aware end-to-end delay
model}\label{sec:delay_model}

The end-to-end latency between a power bus~$b$ and a control
centre~$c$ is, in industrial practice, the sum of four bounded
components: a measurement-acquisition term~$\tau_m$ at the field
side; an edge-processing term~$\tau_e$ inside the substation
gateway; a transmission term~$\tau_t$ along the cyber path; and an
actuation term~$\tau_a$ at the receiving plant
\citep{NASPI_LatencyGuidelines,IEEE_C37_118_2}.  Our delay model
makes each of these four components individually computable, with
role-dependent service times for CC and non-CC nodes that mirror the
processing asymmetry of SCADA/EMS architectures
\citep{Kundur2017CPPS}.

\paragraph{Per-link transmission delay.}
For a single surviving cyber link $(u,v)$, the per-hop transmission
delay is
\begin{equation}\label{eq:link_delay}
  T_{\text{link}}(u,v) \;=\; \frac{S}{R} \;+\; \frac{d_{uv}}{V},
\end{equation}
where $S$ is the packet size in bits, $R$ is the link data rate, $d_{uv}$
is the effective fibre distance and $V$ is the propagation speed.  The
first term is serialisation, the second is propagation.  Uplink and
downlink traffic carry different packet sizes---measurement frames
are larger than command frames, $S_{\text{up}}=1024\,\text{B}$ vs.\
$S_{\text{down}}=256\,\text{B}$ in our base configuration---so the
same physical link contributes asymmetrically to the two directions
even before any role effects are added.  The reference link
parameters used throughout this paper, $R=10\,\text{Mbps}$ and
$V=2{\times}10^{5}\,\text{km/s}$, are within the IEEE C37.118.2
typical-installation band \citep{IEEE_C37_118_2}.

\paragraph{Per-hop edge-processing delay.}
At each intermediate cyber node~$u$ along a path, a forwarding
service delay~$\tau_{\text{fwd}}(u)$ is incurred:
\begin{equation}\label{eq:fwd}
  \tau_{\text{fwd}}(u) \;=\;
  \begin{cases}
    \tau_{\text{CC}}^{\text{fwd}},   & u\text{ is a CC},\\[2pt]
    \tau_{\text{nonCC}}^{\text{fwd}},& \text{otherwise}.
  \end{cases}
\end{equation}
Control centres are equipped with carrier-grade routing hardware
and therefore exhibit a substantially lower per-packet service
latency than ordinary substation gateways; this is the source of
the role-awareness that distinguishes our model from a single-rate
queueing fabric.  The same role distinction applies to the
endpoint contributions $\tau^{\text{tx}}$ and $\tau^{\text{rx}}$ at
the originating and terminating nodes.  Reference values used in
the simulations are reported in Table~\ref{tab:service_times};
they place CC service times at one quarter of the corresponding
non-CC times, consistent with the order-of-magnitude gap between
EMS-grade routers and substation IEDs reported by
\citep{NASPI_LatencyGuidelines}.

\begin{table}[t]
  \centering
  \caption{Reference role-aware service times used in the
    end-to-end delay model.  All values in seconds.  CC and non-CC
    rows scale jointly with the scenario factor of
    Section~\ref{sec:scenarios}; the values shown are for the
    \texttt{baseline} scenario (\textit{scale}~$=1.0$).}
  \label{tab:service_times}
  \small
  \begin{tabular}{@{}lccc@{}}
    \toprule
    Node role & $\tau^{\text{tx}}$ & $\tau^{\text{rx}}$ &
                $\tau^{\text{fwd}}$\\
    \midrule
    Control centre (CC)         & 0.003 & 0.004 & 0.003\\
    Substation gateway (non-CC) & 0.012 & 0.009 & 0.012\\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Path-level aggregation.}
For a multi-hop path $\pi=(u_0,u_1,\dots,u_k)$ in $G_C$ between a
non-CC source and a CC, or vice versa, the total path delay sums
the link, endpoint and forwarding contributions.  Writing the
intermediate nodes as $u_1,\dots,u_{k-1}$,
\begin{align}
  T_{\text{down}}(\pi) &=
    \sum_{i=0}^{k-1}T_{\text{link}}^{\text{down}}(u_i,u_{i+1})
    + \tau_{\text{CC}}^{\text{tx}} + \tau_{\text{nonCC}}^{\text{rx}}
    + \!\!\sum_{j=1}^{k-1}\!\tau_{\text{fwd}}(u_j),
    \label{eq:Tdown}\\[2pt]
  T_{\text{up}}(\pi) &=
    \sum_{i=0}^{k-1}T_{\text{link}}^{\text{up}}(u_i,u_{i+1})
    + \tau_{\text{nonCC}}^{\text{tx}} + \tau_{\text{CC}}^{\text{rx}}
    + \!\!\sum_{j=1}^{k-1}\!\tau_{\text{fwd}}(u_j),
    \label{eq:Tup}
\end{align}
for the downlink (CC$\to$non-CC) and uplink (non-CC$\to$CC)
directions respectively.  The same physical path therefore yields a
different uplink and downlink delay because of (a) directional
packet-size asymmetry in $T_{\text{link}}$, (b) the directional
endpoint terms ($\tau_{\text{CC}}^{\text{tx}}$ vs.\
$\tau_{\text{nonCC}}^{\text{tx}}$), and (c) role-dependent
$\tau_{\text{fwd}}$ at intermediate nodes; not because of any
asymmetric propagation physics on the link itself.

\paragraph{Mapping to the four-component decomposition.}
The four components named in Contribution~2 of the Introduction
correspond to the quantities defined above as follows.  The
transmission term~$\tau_t$ is the path-level link sum
$\sum_i T_{\text{link}}(u_i,u_{i+1})$.  The edge-processing
term~$\tau_e$ is the path-level forwarding sum
$\sum_j \tau_{\text{fwd}}(u_j)$ together with the appropriate
endpoint receive contribution.  The measurement
term~$\tau_m$ aggregates the field-side acquisition delay
$\tau_{\text{PB}\to\text{nonCC}}$ at the substation interface with the
uplink path delay $T_{\text{up}}(\pi_i)$ feeding the nearest CC.
The actuation term~$\tau_a$ aggregates the downlink path delay
$T_{\text{down}}(\pi_i)$ from the controlling CC with the field-side
execution delay $\tau_{\text{nonCC}\to\text{PB}}$.  The two field-side
contributions, set to $0.10\,\text{s}$ and $0.12\,\text{s}$
respectively in the \texttt{baseline} scenario, are anchored against
the round-trip SCADA RTU acquisition+execution budget reported in
\citep{NASPI_LatencyGuidelines} and are scaled jointly with the
cyber service times under each severity scenario of
Section~\ref{sec:scenarios}.  The five scenarios collectively span
the range from an ideal channel ($\tau_m+\tau_e=0$) to the
\texttt{heavy} regime
($\tau_m+\tau_e\!\approx\!594\,\text{ms}$, ${\sim}60\%$ of the
NERC PRC-002-2 sub-second event-reporting upper bound
\citep{NERC_PRC002}); see Section~\ref{sec:scenarios} for the full
parameterisation.

\paragraph{Why four components and not a single $\tau$.}
A model in which $\tau_m$, $\tau_e$, $\tau_t$ and $\tau_a$ collapse
into a single scalar latency cannot, by construction, distinguish
a long-distance fibre regime ($\tau_t$ dominates, $\tau_e$ small,
loss probability low) from a congested edge regime ($\tau_e$
dominates, $\tau_t$ small, loss probability high).  Yet these two
regimes have markedly different consequences for closed-loop
control, because the loss probability per hop scales with $\tau_e$
through the queueing-induced congestion term of
Section~\ref{sec:cascade} and not with $\tau_t$.  Carrying the four
components separately is therefore the minimum that lets the
control-efficacy function $\eta^{+}$ in Section~\ref{sec:cascade}
attribute observed vulnerability to a specific physical channel.

%% ---- 2.3 Interdependent Coupling ------------------------------------
\subsection{Interdependent coupling}\label{sec:coupling}

The two layers are coupled through a one-to-one map between power
buses and non-CC cyber nodes, encoded in a binary coupling matrix
$A_{pc}\in\{0,1\}^{|V_P|\times(|V_C|-n_{\text{cc}})}$ such that
$A_{pc}(b,n)=1$ when bus $b\in V_P$ is monitored by non-CC node
$n\in V_C\setminus\mathcal{C}$ and zero otherwise, where
$\mathcal{C}$ denotes the set of $n_{\text{cc}}$ control centres.
Control centres are not coupled to any power bus directly; they
exchange traffic with the field only via the substation gateways
they control, in line with the layered architecture mandated by
NERC CIP-012 \citep{NERC_CIP012}.

\paragraph{Betweenness-assortative pairing.}
$A_{pc}$ is constructed by sorting the power buses in descending
order of their betweenness centrality in $G_P$, independently
sorting the non-CC cyber nodes in descending order of their
betweenness centrality in $G_C$, and pairing them rank by rank.
The rationale is operational: structurally critical buses---those
whose removal would fragment the largest number of source--sink
paths in the power graph---are the buses for which a utility
provisions the most capable monitoring and the highest-priority
communication channels, and these are correspondingly placed on
the structurally critical communication nodes.  Random or
disassortative coupling rules are also defensible in principle,
but they describe networks that have not been engineered with
operational priority in mind and would therefore systematically
overstate the cyber-physical fragility of a real grid.

\paragraph{Bidirectionality of the coupled channel.}
Crucially, the coupling map is used twice in every cascade round:
(i)~upwards, to deliver the field-side measurement of bus~$b$ along
the uplink path~$\pi_b$ to the nearest surviving CC, contributing
to $\tau_m$ via Eq.~\eqref{eq:Tup}; and (ii)~downwards, to deliver
the dispatch command from that CC back to the substation gateway
for execution at bus~$b$, contributing to $\tau_a$ via
Eq.~\eqref{eq:Tdown}.  The single coupling map therefore carries
both directions of the closed-loop control channel, which is
precisely what allows a structural failure in $G_C$ to lengthen
\emph{both} $\tau_m$ and $\tau_a$ for a given generator and
thereby couple cyber-side damage into the power-side dispatch
within the same cascade round.

\paragraph{Scope of the contribution claimed.}
The pairing rule above is a re-statement of standard practice in
the interdependent-networks literature
\citep{Buldyrev2010,Brummitt2012,ChenTu2026} and is not itself
claimed as a contribution of the present paper.  What is novel,
and what is set up in Section~\ref{sec:cascade}, is the use of
this same map to carry the four-component delay budget into the
generator dispatch \emph{before} the DC power-flow redistribution
of each cascade round, so that the structural state of $G_C$
modulates the electrical state of $G_P$ in the same iteration.

%% ---- 2.4 Capacity-Tolerance Model -----------------------------------
\subsection{Capacity--tolerance model}\label{sec:capacity}

Overload-driven cascading requires a model of component capacity.
We adopt the standard tolerance-factor formulation of
\citet{Motter2002}, applied symmetrically to power branches, cyber
nodes and cyber edges:
\begin{align}
  C_e^{P}   &= (1+\alpha)\,\bigl|P_e^{(0)}\bigr|, &
  e&\in E_P, \label{eq:cap_P}\\
  C_i^{C}   &= (1+\alpha)\,B_i^{(0)}, &
  i&\in V_C, \label{eq:cap_Cn}\\
  C_e^{CE}  &= (1+\alpha)\,B_e^{(0)}, &
  e&\in E_C, \label{eq:cap_Ce}
\end{align}
where $P_e^{(0)}$ is the initial DC-PF flow on power branch~$e$,
$B_i^{(0)}$ the initial node-betweenness of cyber node~$i$ and
$B_e^{(0)}$ the initial edge-betweenness of cyber link~$e$.

The single tolerance parameter $\alpha\in[0,1]$ governs the excess
capacity of every component: at $\alpha=0$ each component operates
at exactly its initial load and any redistribution will trip it;
at $\alpha=1$ each component can absorb up to twice its initial
load before tripping.  A common $\alpha$ across the two layers is
adopted because the two layers are typically built and reinforced
to a common operational margin, and because using two independent
tolerances would expand the experimental design from a one-dimensional
sweep to a two-dimensional surface for no clear physical
gain---existing CPPS literature is essentially unanimous on this
choice \citep{Motter2002,Buldyrev2010,Cai2016TSG}.  Throughout the
experiments of Section~\ref{sec:experiment}, $\alpha$ is swept in
increments of~0.1 over the closed interval $[0,1]$, giving the
eleven tolerance levels reported there.

A subtle point is that, because $C_i^{C}$ and $C_e^{CE}$ depend on
the \emph{initial} betweenness while overload is checked against
the \emph{updated} betweenness on the surviving cyber graph, a node
or edge whose role becomes more central after a structural failure
will see its load grow against a frozen capacity and may then
overload---this is the cyber-side analogue of the power-side
flow-redistribution overload of \citet{Motter2002} and is the
mechanism through which a single targeted cyber attack can fan out
into a multi-component cyber cascade in Section~\ref{sec:cascade}.
%% =====================================================================
%%  END SECTION 2 v1
%%
%%  Synchronisation notes for paper2_delay_cascade.tex:
%%   * Replace lines 224-404 of paper2_delay_cascade.tex (the existing
%%     \section{Cyber-Physical Power System Model} ... up to and
%%     including the Capacity-Tolerance subsection) with the body
%%     above.  The numerical content matches createDelayConfig.m and
%%     createDelayScenarioConfigs.m exactly.
%%   * The reference \citep{Athay1979} (IEEE 39-bus origin paper),
%%     \citep{Pagani2013} (utility-grade communication topology
%%     review) and \citep{Fortunato2010} (community / cohesion in
%%     networks) must be added to paper2_refs_v2_additions.bib if not
%%     already present.  All other citations used here are already
%%     in paper2_refs.bib or paper2_refs_v2_additions.bib.
%% =====================================================================


%% =====================================================================
%%  SECTION 3 -- Delay-Integrated Cascading Failure Dynamics  (v1)
%%
%%  Design notes (kept consistent with the v3 Introduction / Section 2):
%%   * Single-narrative style; no per-step \paragraph{Step 1}\ldots
%%     enumerations.  The companion file paper2_delay_cascade.tex
%%     (lines 410--620) writes the same procedure as a numbered
%%     four-step recipe; that level of operational detail is
%%     deliberately suppressed here because (i) the procedure itself
%%     is standard cascade machinery (\citet{Motter2002,
%%     Buldyrev2010, Parshani2010}) and (ii) the contribution lies in
%%     how communication delay enters that machinery as an endogenous
%%     causal variable, which is what this section foregrounds.
%%   * Mechanism-specific objects forward-referenced from Section 2
%%     are introduced here once and only once: the three-factor
%%     control efficacy $\eta^{+}$ with its three $\alpha$-levers,
%%     the delay-coupled UFLS over-shedding rule, the
%%     cascade-induced CC queueing delay $\tau_q$, the five
%%     severity scenarios, and the resilience metrics $R_1$, $R_3$.
%%     Numerical values match createDelayConfig.m, computeEtaPlus.m,
%%     createDelayScenarioConfigs.m and cascadeLogicdebug2gudingCC_bet_8.m
%%     of the present implementation.
%% =====================================================================
\section{Delay-Integrated Cascading Failure Dynamics}\label{sec:cascade}

In a conventional cascade model every generator delivers its full
scheduled output regardless of communication quality, so the cyber
state of $G_C$ enters the calculation only as a binary mask
(``which generators are reachable?'') after the electrical
redistribution has been computed.  We instead treat the cyber state
as an endogenous causal variable: at every cascade round, the
surviving cyber graph determines a per-generator delay, the delay
determines a per-generator control efficacy, the efficacy scales the
generator injection \emph{before} the DC power flow, and the
resulting branch flows determine which physical and cyber components
fail next.  The same map $A_{pc}$ that defined the interlayer
coupling in Section~\ref{sec:coupling} is therefore reused as the
control-loop carrier; communication delay is no longer a post-hoc
diagnostic of an already-completed cascade but a closed-loop driver
of the cascade trajectory itself
\citep{Buldyrev2010,Parshani2010,Cai2016TSG}.  The remainder of this
section makes this loop concrete in five steps---initial disturbance
and dual-loop iteration (Section~\ref{sec:cascade_loop}), the
delay-aware dispatch through the $\eta^{+}$ function
(Section~\ref{sec:delay_dispatch}), the delay-coupled UFLS rule
(Section~\ref{sec:ufls}), the cascade-induced CC queueing delay
(Section~\ref{sec:tauq}), the five severity scenarios used in the
experiments (Section~\ref{sec:scenarios})---and closes with the two
resilience metrics through which the loop is observed
(Section~\ref{sec:metrics}).

%% ---- 3.1 Attack and dual-loop architecture --------------------------
\subsection{Initial disturbance and dual-loop iteration}\label{sec:cascade_loop}

Each simulation is initialised by a betweenness-targeted attack on the
cyber layer: the single non-CC node with the highest initial node
betweenness in $G_C$ is removed.  Targeting a high-betweenness relay
maximises the immediate disruption to communication paths, lengthens
many delay-weighted shortest paths from generators to control centres,
and---if the attacked node lies on the only path between a CC and a
sub-region---may fragment $G_C$ into reachable and unreachable
components in a single shot \citep{Albert2000,Pagani2013}.  All
subsequent failures are produced endogenously by the cascade, not by
exogenous removals; the targeted attack therefore plays the role of
the seed disturbance studied in classical cascade analyses
\citep{Motter2002,Dobson2007}.

The cascade itself iterates a nested dual-loop architecture.  The
\emph{inner loop} resolves structural propagation between the two
layers: it identifies and removes any cyber component that lacks a
surviving control centre (the Functional Viability-Constrained Island
Detection mechanism FVC-ID introduced in our prior work
\citep{ChenTu2026}), propagates failures between $V_C$ and $V_P$ via
the coupling map $A_{pc}$ with probability~$p$, and removes any power
component that has lost both generation and load.  The inner loop
runs to a fixed point.  The \emph{outer loop} then performs one
overload sweep on the resulting surviving network: cyber-side
betweenness is recomputed and components whose updated load exceeds
their frozen capacity (Eqs.~\eqref{eq:cap_Cn}--\eqref{eq:cap_Ce}) are
tripped; on the power side the delay-aware dispatch of
Section~\ref{sec:delay_dispatch} sets every surviving generator's
output to $\eta_i\,P_i^{(0)}$, the under-frequency rule of
Section~\ref{sec:ufls} rebalances load against the resulting
generation, a DC power flow is solved on the surviving topology, and
any branch with $|P_e|>C_e^{P}$ from Eq.~\eqref{eq:cap_P} is
tripped.  Newly failed components are returned to the inner loop and
the procedure repeats until no further failures occur.  This is the
same dual-loop skeleton used by \citet{Cai2016TSG} and
\citet{Parshani2010}; the architectural innovation is purely the
delay-aware generator scaling that takes place \emph{between} the
inner-loop fixed point and the DC power flow.

%% ---- 3.2 Delay-aware generator dispatch -----------------------------
\subsection{Delay-aware generator dispatch through
\texorpdfstring{$\eta^{+}$}{eta+}}\label{sec:delay_dispatch}

For every generator $i$ that survives the inner loop, the framework
first locates its cyber image $\pi_i\in V_C$ via $A_{pc}$ and then
solves a delay-weighted shortest-path problem from $\pi_i$ to the
nearest surviving control centre on the residual cyber graph,
weighting each edge by the link delay
$T_{\text{link}}(u,v)$ of Section~\ref{sec:cyber}.  Generators whose
cyber image is disconnected from every surviving CC are declared
\emph{unreachable}: they receive no measurement uplink and no
dispatch downlink, and their injection is set to zero before the DC
power flow, which is the most severe single-step consequence of
cyber-layer fragmentation.  All remaining generators receive a
measurement delay
\begin{equation}\label{eq:tau_m_v3}
  \tau_{m,i}=\tau_{\text{PB}\to\text{nonCC}}
              +T_{\text{up}}(\pi_i)+\tau_q(r),
\end{equation}
and an execution delay
\begin{equation}\label{eq:tau_e_v3}
  \tau_{e,i}=\tau_{\text{nonCC}\to\text{PB}}
              +T_{\text{down}}(\pi_i)+\tau_q(r),
\end{equation}
where $T_{\text{up}}/T_{\text{down}}$ are the four-component
end-to-end path delays defined in Eqs.~\eqref{eq:Tup}--\eqref{eq:Tdown},
$\tau_{\text{PB}\to\text{nonCC}}$ and $\tau_{\text{nonCC}\to\text{PB}}$
are the field-side measurement and execution interface delays of
Section~\ref{sec:coupling}, and $\tau_q(r)$ is the cascade-induced
control-centre queueing delay defined in Section~\ref{sec:tauq}.

The two delays $\tau_{m,i},\tau_{e,i}$ are converted to a per-generator
control efficacy $\eta_i\in[0,1]$ through the three-factor function
$\eta^{+}$:
\begin{equation}\label{eq:etaplus}
  \eta_i \;=\; \Phi^{\text{sat}}_i \cdot \Phi^{\text{loss}}_i \cdot
               \Phi^{\text{crit}}_i .
\end{equation}
The three factors decompose the influence of delay along three
qualitatively distinct physical channels.  $\Phi^{\text{sat}}_i$
captures \emph{control-loop saturation}: it is unity inside an
engineering dead-band $(\tau_{m0},\tau_{e0})$---typical of the
PMU reporting interval permitted by IEEE~C37.118.1
\citep{IEEE_C37_118_2} and of governor / AVR action dead-bands---and
decays exponentially once the dead-band is exceeded,
\begin{equation}\label{eq:phi_sat}
  \Phi^{\text{sat}}_i = \exp\!\bigl[
       -a_m\,(\tau_{m,i}-\tau_{m0})^{+}
       -a_e\,(\tau_{e,i}-\tau_{e0})^{+}
     \bigr],
\end{equation}
with $(x)^{+}=\max(0,x)$.  $\Phi^{\text{loss}}_i$ captures
\emph{cyber-path packet loss}: each hop has an effective drop
probability $p_h^{\text{eff}}=p_h\cdot\min(1,(\tau_{m,i}+\tau_{e,i})/\tau_{\text{ref}})$
that grows monotonically with end-to-end delay---an
empirical regularity recovered from M/M/1 queueing analysis
\citep{Kleinrock1975} and reflected in ITU-T~G.1010 traffic-class
guidance---and the per-path loss accumulates over the
$n_{\text{hops}}^{(i)}$ hops of the chosen route.
$\Phi^{\text{crit}}_i$ captures \emph{generator-specific critical
stability}: a logistic gate with
$\tau_{\text{crit},i}=\tau_{\text{crit}}^{\max}\cdot r_i$ that
declines steeply once total delay exceeds a per-machine threshold
proportional to the generator's relative capacity
$r_i=P_i^{(0)}/\max_j P_j^{(0)}$, normalised so that
$\Phi^{\text{crit}}_i(\tau{=}0)=1$.  The capacity-weighted
threshold is the model-level expression of the well-known
asymmetry that small machines have less inertia and tighter
frequency tolerance windows than large machines
\citep{Kundur2017CPPS}.  Because the three factors act on
qualitatively independent channels, every reachable generator at
zero delay satisfies $\Phi^{\text{sat}}_i=\Phi^{\text{loss}}_i=
\Phi^{\text{crit}}_i=1$ and hence $\eta_i=1$, so the
\nodelay{} scenario of Section~\ref{sec:scenarios} is a strict
upper bound on every realisation of the cascade.

The single tolerance parameter $\alpha$ from
Section~\ref{sec:capacity} enters $\eta^{+}$ along three
parallel levers that reflect the engineering reality that a
better-resourced grid can also afford a better-resourced
control loop.  First, $\Phi^{\text{loss}}_i$ is replaced by its
$k$-fold parallel-redundancy reliability with
$k_{\text{eff}}(\alpha)=1+\alpha\,(k_{\max}-1)$, mapping
$\alpha$ to the equivalent number of independent CC$\leftrightarrow$gen
channels (IEEE~Std~493 / IEC~61850 PRP / NERC~CIP-012
\citep{NERC_CIP012,IEC_61850_PRP}); second, $\tau_{m0},\tau_{e0}$
in Eq.~\eqref{eq:phi_sat} are widened linearly in~$\alpha$, mirroring
the C37.118 PMU and IEC~60255-181 UFLS dead-band relaxation that
high-reserve systems can adopt without loss of compliance; third,
$\tau_{\text{crit}}^{\max}$ is widened linearly in~$\alpha$ to
reflect the wider frequency-response tolerance windows admitted by
NERC~PRC-006-5 \citep{NERC_PRC006} and the NASPI WAMS
implementation guidance \citep{NASPI_LatencyGuidelines} for systems
with deep operating reserves.  All three levers degenerate cleanly
at $\alpha=0$, so the single-tolerance interpretation of
Section~\ref{sec:capacity} remains intact.  The output of this
subsection is the scaled injection
\begin{equation}\label{eq:Pactual_v3}
  P_i^{\text{actual}} \;=\; \eta_i \cdot P_i^{(0)},
\end{equation}
which replaces $P_i^{(0)}$ in the DC power flow of the outer
loop and is the single point at which communication delay alters
the electrical state of $G_P$.

%% ---- 3.3 Delay-coupled UFLS over-shedding ---------------------------
\subsection{Delay-coupled under-frequency load shedding}\label{sec:ufls}

The injection scaling of Eq.~\eqref{eq:Pactual_v3} produces, in
general, a per-round generation deficit
$\Phi^{\text{glob}}=\sum_i P_i^{\text{actual}}/\sum_i P_i^{(0)}\le 1$
that the DC power flow alone would close by drawing the missing
power from the slack bus.  This numerical artefact would route the
deficit through paths that bear no relation to the physical response
of a real grid, in which insufficient generation triggers an
under-frequency event answered by the load-shedding (UFLS) schemes
mandated by NERC~PRC-006-5 \citep{NERC_PRC006},
IEEE~Std~C37.117 and IEC~60255-181.  We therefore close the
power balance physically rather than numerically: every load bus is
scaled by an effective shedding factor $\Phi^{\text{eff}}$ before
the DC power flow is solved, so that the post-shed total load
matches the post-$\eta$ generation.

Communication delay enters this rule because the UFLS scheme itself
relies on the same cyber channels: the frequency measurement is
delayed, the trip command is delayed, and standard practice
(IEEE~Std~C37.117, NERC~PRC-006) recommends a 20--50\%
\emph{over-shedding margin} to compensate for the resulting
under-estimation of the actual frequency dip.  We model this margin
as a normalised function of the same end-to-end delay as
$\Phi^{\text{loss}}$,
\begin{equation}\label{eq:gamma_ufls}
  \gamma(\bar\tau)
   = \gamma_{\max}\cdot\min\!\Bigl(1,\;\bar\tau/\tau_{\text{ref}}^{\text{UFLS}}\Bigr),
\end{equation}
with $\bar\tau$ the mean of $\tau_{m,i}+\tau_{e,i}$ over reachable
generators.  The shedding factor is then
\begin{equation}\label{eq:phi_eff}
  \Phi^{\text{eff}}
    = \max\!\Bigl(0,\;1-\min\!\bigl(\bar s_{\max}(\alpha),\,
                  (1-\Phi^{\text{glob}})(1+\gamma(\bar\tau))\bigr)\Bigr),
\end{equation}
in which the inner clamp $\bar s_{\max}(\alpha)$ is the per-event
shed cap recommended by NERC~PRC-006-5 (single-event UFLS
$\le0.85$ for low-reserve operation, narrowed linearly to $0.75$
at $\alpha=1$ to reflect that high-reserve systems perform
shallower single-stage shedding).  At zero delay,
$\gamma=0$ and Eq.~\eqref{eq:phi_eff} reduces to the conventional
shed-equal-to-deficit rule.  As delay grows, the same cyber
mechanism that lowered $\eta_i$ now also forces the system to
shed more load than the instantaneous deficit would justify; this
makes load shedding a second, structurally distinct channel through
which delay degrades $R_1$, complementary to the generator scaling
of Section~\ref{sec:delay_dispatch}.

%% ---- 3.4 Cascade-induced CC queueing delay --------------------------
\subsection{Cascade-induced control-centre queueing delay}\label{sec:tauq}

The end-to-end path delays $T_{\text{up}},T_{\text{down}}$ in
Section~\ref{sec:cyber} were defined for an unloaded network.  When
the cascade has removed one or more control centres, the surviving
CCs must absorb the full measurement and dispatch traffic of the
generators that remain in service, and finite-capacity queueing
inside each PDC begins to dominate the latency budget.  Following
\citet{Kleinrock1975} and the PDC service-rate envelope reported
by IEEE~C37.247 / NASPI WAMS implementation guidance
\citep{NASPI_LatencyGuidelines}, we model each surviving CC as an
M/M/1 server with service rate $\mu_{\text{cc}}^{\text{eff}}(\alpha)
=\mu_{\text{cc}}\bigl(1+g_{\mu}\,\alpha\bigr)$ and obtain a
per-round mean wait
\begin{equation}\label{eq:tauq}
  \tau_q(r) \;=\; \frac{1}{\mu_{\text{cc}}^{\text{eff}}(\alpha)}\cdot
                 \frac{\rho_{\text{eff}}(r)}{1-\rho_{\text{eff}}(r)},
\end{equation}
with utilisation
$\rho(r)=\bigl(N_{\text{gen}}(r)\,\lambda_g\,s_{\text{scn}}\bigr)/\bigl(N_{\text{cc}}(r)\,\mu_{\text{cc}}^{\text{eff}}\bigr)$
and $\rho_{\text{eff}}=\min(\rho,\rho_{\max})$ to enforce the
NERC~PRC-005 engineering ceiling that real PDC clusters offload to
backup once $\rho>0.95$.  The scenario factor $s_{\text{scn}}$
inherits the per-scenario scale of Section~\ref{sec:scenarios}, so
that $\tau_q$ vanishes identically in the \nodelay{} scenario.
$\tau_q(r)$ is symmetrically added to both $\tau_{m,i}$ and
$\tau_{e,i}$ in Eqs.~\eqref{eq:tau_m_v3}--\eqref{eq:tau_e_v3} and
therefore propagates back into $\eta^{+}$.  Conceptually, this
closes a second feedback path: cyber failures shrink the CC
service capacity, which raises queueing delay, which lowers
$\eta^{+}$, which reroutes more load through under-frequency
shedding and triggers further failures.

%% ---- 3.5 Five severity scenarios ------------------------------------
\subsection{Delay severity scenarios}\label{sec:scenarios}

The five delay severity scenarios used throughout
Section~\ref{sec:experiment} are defined by coherently rescaling a
single baseline parameter set (Table~\ref{tbl:scenarios_v3}).  The
scale factor $s$ multiplies the uplink and downlink packet sizes,
the link distance, every service-delay component
$\tau_{\text{tx}},\tau_{\text{rx}},\tau_{\text{fwd}}$, and both
field-side interface delays
$\tau_{\text{PB}\to\text{nonCC}},\tau_{\text{nonCC}\to\text{PB}}$,
while the link rate~$R$, the propagation speed~$V$, and the
sensitivity coefficients $a_m,a_e$ inside $\eta^{+}$ remain fixed.
This is an engineering-motivated stress family, not a literal
geometric rescaling: the five scales are anchored to documented
WAMS / IEC / NERC operating bands so that each scenario maps to a
realistic operating regime rather than to an arbitrary point on a
continuous sweep.

\begin{table}[t]
\caption{Delay severity scenarios.  Total measurement-plus-execution
delay $\tau_{\text{tot}}=\tau_{m,i}+\tau_{e,i}$ at the cyber-path
component evaluated on the unattacked network; the three central
scales are anchored to the IEEE~C37.118 PMU reporting band
\citep{IEEE_C37_118_2}, the IEC~61850-90-5 inter-area class-M2/M3
band, the NASPI cross-region P95 measurement
\citep{NASPI_LatencyGuidelines} and the NERC~PRC-002-2 upper
bound \citep{NERC_PRC002} respectively.}\label{tbl:scenarios_v3}
\centering
\begin{tabular}{@{}lccl@{}}
\toprule
Scenario & $s$ & $\tau_{\text{tot}}$ (ms) & Operating regime \\
\midrule
\nodelay{} & 0.0 &   0 & ideal-channel reference \\
light      & 0.5 & 110 & local-area WAMS, IEEE~C37.118 PMU stream \\
baseline   & 1.0 & 220 & inter-area WAMS, IEC~61850-90-5 M2/M3 \\
medium     & 1.7 & 374 & NASPI cross-region P95 measurement band \\
heavy      & 2.7 & 594 & WAN-congested, $\sim$60\% of NERC PRC-002-2 cap \\
\bottomrule
\end{tabular}
\end{table}

The \nodelay{} scenario plays a structural role beyond that of a
reference point.  Because $s=0$ zeroes every delay-producing
parameter and the scenario factor $s_{\text{scn}}$ in
Eq.~\eqref{eq:tauq}, every reachable generator satisfies
$\tau_{m,i}=\tau_{e,i}=\tau_q=0$, hence
$\eta_i=1$, hence $\Phi^{\text{glob}}=1$ and
$\Phi^{\text{eff}}=1$, so the cascade reduces exactly to the
classical capacity--tolerance model of \citet{Motter2002} on the
same coupled topology.  Any difference between
\nodelay{} and the four delay-active scenarios at the same
$(\alpha,\text{trial})$ is therefore causally attributable to
communication delay and to the closed-loop interactions it induces.

%% ---- 3.6 Resilience metrics -----------------------------------------
\subsection{Resilience metrics}\label{sec:metrics}

Two complementary metrics are extracted at the terminal cascade
state of every $(\alpha,\text{scenario},\text{trial})$ realisation.
Load retention,
\begin{equation}\label{eq:R1_v3}
  R_1
  \;=\; \frac{\sum_{b\in V_P^{\text{surv}}}\Phi^{\text{eff}}\,P_b^{(0)}}
             {\sum_{b\in V_P}P_b^{(0)}},
\end{equation}
measures the fraction of pre-attack load that remains served once
the cascade has terminated and after the delay-coupled UFLS rule of
Eq.~\eqref{eq:phi_eff} has been applied.  $R_1$ aggregates every
upstream effect of delay---reduced $\eta_i$, deeper UFLS shedding,
additional overload tripping---into a single bus-weighted survival
ratio.  Dispatch fidelity,
\begin{equation}\label{eq:R3_v3}
  R_3
  \;=\; \sqrt{\frac{\sum_{j\in V_G^{\text{surv}}}P_j^{(0)}\,
            \bigl(P_j^{\text{actual}}-P_j^{(0)}\bigr)^{2}/{P_j^{(0)}}^{2}}
                {\sum_{j\in V_G^{\text{surv}}}P_j^{(0)}}},
\end{equation}
is a capacity-weighted normalised RMS of the relative deviation
between each surviving generator's delay-degraded output and its
reference dispatch; unreachable generators contribute the maximum
relative deviation of unity.  $R_3$ measures a quality of operation
that aggregate load retention cannot expose: a system can hold
$R_1$ near unity while leaking control authority through the
$\eta^{+}$ channel, and that hidden degradation surfaces only in
$R_3$.  In the language of recent CPPS resilience taxonomies
\citep{Panteli2015,Bie2017}, $R_1$ is a quantitative survivability
indicator and $R_3$ is a qualitative controllability indicator;
together they bracket the two failure modes through which
communication delay damages a coupled cyber-physical grid, and
they are the two observables on which Section~\ref{sec:experiment}
evaluates every claim made in this paper.

%% =====================================================================
%%  END SECTION 3 v1
%%
%%  Compilation notes:
%%   * The macro \nodelay{} (rendering "no\_delay" in upright \texttt)
%%     is defined via \providecommand near the top of this file so the
%%     section compiles standalone; if paper2_introduction_v3.tex is
%%     later \input together with paper2_delay_cascade.tex (line 19),
%%     the \providecommand will defer to the existing definition.
%%   * Eq. labels eq:Tup, eq:Tdown, eq:cap_P, eq:cap_Cn, eq:cap_Ce
%%     are defined in Section 2 of this same file; do not redefine.
%%   * The reference \citep{Kleinrock1975} (M/M/1 queueing) and
%%     \citep{IEC_61850_PRP} (PRP redundancy) must be present in
%%     paper2_refs_v2_additions.bib.  The remaining citations
%%     (ChenTu2026, Motter2002, Buldyrev2010, Parshani2010,
%%     Cai2016TSG, Albert2000, Pagani2013, Dobson2007, Kundur2017CPPS,
%%     IEEE_C37_118_2, NERC_PRC002, NERC_PRC006, NERC_CIP012,
%%     NASPI_LatencyGuidelines, Panteli2015, Bie2017) are already
%%     in paper2_refs.bib or paper2_refs_v2_additions.bib.
%% =====================================================================
